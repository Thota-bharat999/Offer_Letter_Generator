<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Relieving and Experience Letter</title>
  <style>
    @page {
      size: A4;
      margin: 0;
    }

    body {
      font-family: 'Calibri', Arial, sans-serif;
      font-size: 12px;
      line-height: 1.75;
      color: #000;
      margin: 0;
      padding: 0;
      background: url("/assets/letterhead.jpeg") no-repeat center top;
      background-size: cover;
    }

    .content {
      position: relative;
      width: 78%; /* ✅ slightly narrower for balanced left/right margins */
      margin: 0 auto;
      top: 160px;
      font-size: 13px;
      line-height: 1.8;
    }

    .date {
      text-align: left;
      margin-bottom: 18px;
    }

    .title {
      text-align: center;
      font-weight: bold;
      text-decoration: underline;
      font-size: 16px;
      margin-top: 0;
      margin-bottom: 45px;
    }

    .gap-after-title {
      height: 35px;
    }

    .paragraph {
      margin: 0 0 9pt 0;
      text-align: justify;
    }

    .employee-section {
      margin-bottom: 20px;
    }

    /* ✅ Fixed Signature Section */
    .signature {
      margin-top: 40px;
      margin-left: 25mm; /* ✅ consistent left indent */
      text-align: left;
      line-height: 1.6;
      font-family: Cambria, Calibri, serif;
      font-size: 13px;
    }

    .signature .sincerely {
      margin-bottom: 6px;
      font-weight: 600;
    }

    .signature .for {
      font-weight: 700;
      margin-bottom: 12px;
    }

    .signature-image {
      margin: -6px 0 6px 10px;
    }

    .signature-image img {
      width: 85px;
      height: auto;
      opacity: 0.95;
    }

    .signature .signer {
      font-weight: 600;
      margin-top: 10px;
      margin-bottom: 2px;
    }

    .signature .designation {
      margin-top: -2px;
      font-weight: bold;
      text-decoration: underline;
    }

    /* ✅ Note style consistent with original */
    .note {
      font-size: 11pt;
      margin-top: 35px;
      margin-left: 25mm;
      text-align: left;
      line-height: 1.4;
    }
  </style>
</head>
<body>
  <div class="content">
    <p class="date">
      <%= new Date(relieving_date).toLocaleDateString('en-GB').replace(/\//g, '-') %>
    </p>

    <p class="title">Relieving and Experience Letter</p>
    <div class="gap-after-title"></div>

    <div class="employee-section">
      <p class="paragraph"><strong>Employee Name:</strong> <%= employee_name %></p>
      <% if (employee_id) { %>
        <p class="paragraph"><strong>Employee ID:</strong> <%= employee_id %></p>
      <% } %>
    </div>

    <p class="paragraph">This is with regard to your resignation.</p>

    <p class="paragraph">
      Dated <%= new Date(resignation_date).toLocaleDateString('en-GB').replace(/\//g, '-') %>
      from the position of <strong><%= designation %></strong> — We would like to inform you that you have been
      relieved of your duties at the end of business hours of
      <%= new Date(relieving_date).toLocaleDateString('en-GB').replace(/\//g, '-') %>.
    </p>

    <p class="paragraph">
      We enjoyed working with you and thank you for the service rendered from
      <strong><%= new Date(start_date).toLocaleDateString('en-GB').replace(/\//g, '-') %></strong>
      to
      <strong><%= new Date(relieving_date).toLocaleDateString('en-GB').replace(/\//g, '-') %></strong>.
    </p>

    <p class="paragraph">We wish you good luck for future endeavours.</p>

    <!-- ✅ Signature section -->
    <div class="signature">
      <p class="sincerely"><strong>Yours sincerely,</strong></p>
      <p class="for"><strong>For <%= companyName %></strong></p>

      <% if (signaturePath) { %>
        <div class="signature-image">
          <img src="<%= signaturePath %>" alt="Authorized Signature" />
        </div>
      <% } %>

      <p class="signer"><strong>Chakravarthy Devarakonda</strong></p>
      <p class="designation"><strong><u>Manager – Human Resources.</u></strong></p>
    </div>

    <p class="note">
      <strong>Note:</strong> This is a computer-generated document. No Signature is required*
    </p>
  </div>
</body>
</html>
