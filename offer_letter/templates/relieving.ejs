<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Relieving and Experience Letter</title>
  <style>
    @page {
      size: A4;
      margin: 0;
    }

    body {
      font-family: 'Calibri', Arial, sans-serif;
      font-size: 12px; /* ✅ Matches original */
      line-height: 1.75; /* ✅ Tighter line spacing like original */
      color: #000;
      margin: 0;
      padding: 0;
      background: url("/assets/letterhead.jpeg") no-repeat center top;
      background-size: cover;
    }

    .content {
      position: relative;
      width: 75%;
      margin: 0 auto;
      top: 165px; /* Same vertical offset */
      font-size: 12.5px;
      line-height: 1.8;
    }

    .date {
      text-align: left;
      margin-bottom: 16px;
    }

    .title {
      text-align: center;
      font-weight: bold;
      text-decoration: underline;
      font-size: 12.5px;
      margin-bottom: 26px; /* ✅ Slightly smaller gap */
    }

    .paragraph {
      margin: 0 0 9pt 0; /* ✅ Reduced spacing between paragraphs */
      text-align: justify;
    }

    .closing {
      margin-top: 16pt;
      line-height: 1.3;
    }

    .signature-block {
      margin-top: 3pt; /* ✅ Tighter gap between 'For Amazon' and stamp */
      text-align: left;
      line-height: 1.2;
    }

    .signature-block img {
      width: 85px;
      height: auto;
      opacity: 0.95;
      margin-top: 2pt;
    }

    .hr-details {
      margin-top: 1pt; /* ✅ Stamp and HR name closer */
      line-height: 1.2;
    }

    .note {
      font-size: 10pt;
      margin-top: 20pt; /* ✅ Slightly smaller gap from HR info */
      text-align: left;
    }
  </style>
</head>
<body>
  <div class="content">
    <p class="date">
      <%= new Date(relieving_date).toLocaleDateString('en-GB').replace(/\//g, '-') %>
    </p>

    <p class="title">Relieving and Experience Letter</p>
    <div style="height: 10px;"></div>

    <p class="paragraph"><strong>Employee Name:</strong> <%= employee_name %></p>
    <% if (employee_id) { %>
      <p class="paragraph"><strong>Employee ID:</strong> <%= employee_id %></p>
    <% } %>

    <p class="paragraph">This is with regard to your resignation,</p>

    <p class="paragraph">
      Dated
      <%= new Date(resignation_date).toLocaleDateString('en-GB').replace(/\//g, '-') %>
      from the position of <strong><%= designation %></strong> — We would like to inform you that you have been relieved of your duties at the end of business hours of
      <%= new Date(relieving_date).toLocaleDateString('en-GB').replace(/\//g, '-') %>.
    </p>

    <p class="paragraph">
      We enjoyed working with you and thank you for the service rendered from
      <strong><%= new Date(start_date).toLocaleDateString('en-GB').replace(/\//g, '-') %></strong>
      to
      <strong><%= new Date(relieving_date).toLocaleDateString('en-GB').replace(/\//g, '-') %></strong>.
    </p>

    <p class="paragraph">We wish you good luck for future endeavours.</p>

    <div class="closing">
      <p>Yours sincerely,</p>
      <p>For Amazon IT Solutions</p>
    </div>

    <% if (signaturePath) { %>
      <div class="signature-block">
        <img src="<%= signaturePath %>" alt="Signature" />
      </div>
    <% } %>

    <div class="hr-details">
      <p><%= hrName %></p>
      <p><strong><%= hrDesignation %></strong></p>
    </div>

    <p class="note">
      Note: This is a computer-generated document. No Signature is required*
    </p>
  </div>
</body>
</html>
